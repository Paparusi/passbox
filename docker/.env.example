# PassBox Docker Self-Hosting Configuration
# Copy this to .env in the docker/ directory before running docker-compose up

# PostgreSQL
POSTGRES_PASSWORD=change-me-to-a-strong-password

# Supabase Connection
# If using hosted Supabase, use your project URL and keys
# If using local Supabase via docker-compose, configure accordingly
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
